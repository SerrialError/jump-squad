import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#2f6891',
    },
    secondary: {
      main: '#3b1a10',
    },
  },
});

export default theme;
